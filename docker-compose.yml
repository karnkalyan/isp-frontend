services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kisan-isp-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
    restart: always
