import type {LucideIcon} from "lucide-react";import {ArrowDownRight,ArrowUpRight,Minus} from "lucide-react";import {Card,CardContent} from "@/components/ui/card";import {cn} from "@/lib/utils";
export function KpiCard({label,value,trend,period,icon:Icon,className}:{label:string;value:React.ReactNode;trend?:number;period?:string;icon?:LucideIcon;className?:string}){const Trend=trend===undefined||trend===0?Minus:trend>0?ArrowUpRight:ArrowDownRight;return <Card className={cn("min-h-32",className)}><CardContent className="p-5"><div className="flex items-start justify-between"><p className="text-sm font-medium text-muted-foreground">{label}</p>{Icon&&<span className="flex size-9 items-center justify-center rounded-[10px] bg-secondary text-primary"><Icon className="size-[18px]"/></span>}</div><p className="data-value mt-3 text-3xl font-semibold tracking-tight text-primary dark:text-foreground">{value}</p>{trend!==undefined&&<div className="mt-2 flex items-center gap-1.5 text-xs"><Trend className={cn("size-3.5",trend>0?"text-[#2F7D5A]":trend<0?"text-[#B4233A]":"text-muted-foreground")}/><span className="font-medium">{Math.abs(trend)}%</span><span className="text-muted-foreground">{period||"vs previous period"}</span></div>}</CardContent></Card>}
