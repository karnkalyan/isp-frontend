import {Sparkles} from "lucide-react";import {Card,CardContent} from "@/components/ui/card";import {Badge} from "@/components/ui/badge";import {Button} from "@/components/ui/button";
export function AiInsightCard({title,children,actionLabel,onAction,approvalRequired=false}:{title:string;children:React.ReactNode;actionLabel?:string;onAction?:()=>void;approvalRequired?:boolean}){return <Card className="border-l-[3px] border-l-[#E11D72] bg-secondary/30"><CardContent className="p-5"><div className="flex items-center gap-2"><Sparkles className="size-4 text-[#E11D72]"/><h3 className="font-heading text-sm font-semibold">{title}</h3><Badge variant="ai" className="ml-auto">AI insight</Badge></div><div className="mt-3 text-sm text-muted-foreground">{children}</div>{actionLabel&&<div className="mt-4 flex items-center gap-3"><Button size="sm" onClick={onAction}>{actionLabel}</Button>{approvalRequired&&<span className="text-xs text-muted-foreground">Approval required</span>}</div>}</CardContent></Card>}
